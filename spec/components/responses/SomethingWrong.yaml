description: Something went wrong
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
        error_code:
          type: string
    examples:
      expected:
        value:
          error: <error reason>
          error_code: <some error code>
